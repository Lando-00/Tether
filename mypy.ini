# Phase 1 keeps mypy separate from pyproject so packaging metadata stays focused; this is intentionally non-strict.
[mypy]
python_version = 3.12
ignore_missing_imports = True
follow_imports = silent
exclude = ^(llm_service|legacy|dist|build)/
