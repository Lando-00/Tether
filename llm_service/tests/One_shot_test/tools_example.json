[
  {
    "type": "function",
    "function": {
      "name": "weather_api",
      "description": "Get current weather for a city",
      "parameters": {
        "type": "object",
        "properties": {
          "city": { "type": "string", "description": "City name, e.g., Dublin" },
          "unit": { "type": "string", "enum": ["celsius", "fahrenheit"] }
        },
        "required": ["city"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "fx_rate",
      "description": "Get the current FX rate for a currency pair like EURUSD",
      "parameters": {
        "type": "object",
        "properties": {
          "pair": { "type": "string", "description": "Currency pair, e.g., EURUSD" }
        },
        "required": ["pair"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "stock_quote",
      "description": "Get the latest stock price for a ticker symbol",
      "parameters": {
        "type": "object",
        "properties": {
          "symbol": { "type": "string", "description": "Stock ticker symbol, e.g., AAPL" },
          "market": { "type": "string", "description": "Stock market, e.g., NASDAQ", "default": "NASDAQ" }
        },
        "required": ["symbol"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "translate_text",
      "description": "Translate text from one language to another",
      "parameters": {
        "type": "object",
        "properties": {
          "text": { "type": "string", "description": "The text to translate" },
          "source_language": { "type": "string", "description": "Source language code, e.g., en" },
          "target_language": { "type": "string", "description": "Target language code, e.g., es" }
        },
        "required": ["text", "target_language"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "recipe_search",
      "description": "Find recipes based on available ingredients",
      "parameters": {
        "type": "object",
        "properties": {
          "ingredients": { 
            "type": "array", 
            "items": { "type": "string" },
            "description": "List of available ingredients"
          },
          "cuisine": { "type": "string", "description": "Cuisine type, e.g., Italian, Mexican" },
          "dietary_restrictions": { 
            "type": "array", 
            "items": { "type": "string" },
            "description": "Dietary restrictions, e.g., vegetarian, gluten-free" 
          }
        },
        "required": ["ingredients"]
      }
    }
  }
]
