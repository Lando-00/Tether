# Tether Environment Configuration
# Copy this file to .env and fill in your actual values
# SECURITY: Never commit .env files with real API keys to version control

# =============================================================================
# Brave Search API (Required for web_search tool)
# =============================================================================
# Get a free API key at: https://api-dashboard.search.brave.com/
# Free tier: 2,000 queries/month, 10 requests/minute
BRAVE_API_KEY=your_api_key_here

# =============================================================================
# Legacy API Keys (Optional - for reference only)
# =============================================================================
# NewsAPI (deprecated, replaced by Brave Search)
# NEWSAPI_KEY=your_newsapi_key_here

# =============================================================================
# Other Service Configuration (Optional)
# =============================================================================
# Override config values using TETHER__ prefix
# Format: TETHER__<section>__<subsection>__<key>=value
# Example: TETHER__server__port=9000
# Example: TETHER__tools__web_search__defaults__country=gb

# =============================================================================
# Security Notes
# =============================================================================
# - All API keys are masked in application logs
# - Keys are never sent to external services except their respective APIs
# - All web search traffic uses HTTPS
# - Local SQLite database stores chat history (data/tether.db)
