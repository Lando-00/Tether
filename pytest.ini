[pytest]
markers =
    integration: mark test as integration test requiring real model
    asyncio: mark test as async test (requires pytest-asyncio)
    e2e: mark test as end-to-end test requiring running server
asyncio_mode = auto
